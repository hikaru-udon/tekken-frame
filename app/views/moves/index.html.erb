<%= render "shared/header" %>
<div class="moves-container">

  <%= form_with(url: search_character_moves_path, local: true, method: :get, class:"search-form") do |f| %>
    <%= f.text_field :keyword, placeholder: "キーワードで検索する", class: "search-input" %>
      <%# <%= f.submit "&#xf002; 検索", class: "search-btn"  %>
      <%# 初期値のform.submit内ではFontAwesomeは表示できないので、使用する場合はform.bottonに置き換え、下記の用意記述する %>
    <%= f.button type:"submit", class:"search-btn" %>
  <% end %>

  <div class="back-btn">
    <%=link_to "Topページ", "/", class:"back" %>
    <%=link_to "全技一覧", character_moves_path, class:"back" %>
  </div>

  <div class="character-name">
    <ruby>
      <%= @character.fullname %>
      <rt><%= @character.english_fullname %></rt>
    </ruby>
  </div>

  <table class="moves-data">
    <tr class="moves-info-category">
      <th class="moves_name">技名</th>
      <th class="command">コマンド</th>
      <th class="damage">ダメージ</th>
      <th class="startup">発生F</th>
      <th class="block">ガードF</th>
    </tr>
    <% @moves.each do |moves| %>
      <tr class="moves-info-contents">
        <td class="moves_name"><%=link_to moves.moves_name, "#", class: "name-link" %></td>
        <td class="command"><%= moves.command %></td>
        <td class="damage"><%= moves.damage %></td>
        <td class="startup"><%= moves.startup %></td>
        <td class="block"><%= moves.block %></td>
      </tr>
    <% end %>
  </table>

  <%= render "shared/page-top" %>  

</div>
<%= render "shared/footer" %>